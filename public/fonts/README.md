# Font files

Place the Stolzl font files here for correct rendering:

- `stolzl_regular.otf` (weight 400)
- `stolzl_book.otf` (weight 450)
- `stolzl_medium.otf` (weight 500)
- `stolzl_bold.otf` (weight 700)

The app will fall back to Inter if these files are missing.
